<div class="row g-3">
  <div>
  {{logged_in.id}}
  </div>
  {{#each boardGameReviews as |review| }}
    <div class="col-12 col-md-6 col-lg-4" id="boardGame">
      <div class="card border-0 bg-dark">
        {{> reviewCard }}
      </div>
    </div>
  {{/each}}
</div>