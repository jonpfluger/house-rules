{{!-- <div class="card">
  <div class="card-header bg-primary text-light">
    <h5 class="card-title">{{this.title}}</h3>
      <h6 class="card-subtitle">Game: {{this.gameTitle}}</h6>
  </div>
  <div class="card-body bg-info text-light">
    <p class="card-text">{{this.body}}</p>
    {{#if img}}
    <img src="{{this.img}}" />
    {{/if}}
    <p class="card-text">Created by: {{this.user.name}} on {{this.date_created}}</p>
  </div>
</div> --}}
<div class="card bg-dark text-light">
  <div>
    <h5 class="card bg-success">{{this.title}}</h5>
  </div>
  <div class="row g-0">
    <div class="col-md-4">
      {{#if img}}
      <img src="{{this.img}}" class="img-fluid rounded-start" alt="game img">
      {{/if}}
    </div>
    <div class="col-md-8">
      <div class="card-body bg-warning text-light">
        {{!-- <h5 class="card-title">{{this.title}}</h5> --}}
        <h6 class="card-subtitle">{{this.gameTitle}}</h6>
        <p class="card-text">{{this.body}}</p>
        <p class="card-text"><small class="text-muted">{{this.user.name}} on {{this.date_created}}</small></p>
      </div>
    </div>
  </div>
</div>