<div class="row g-3">

{{#each reviews}}
<div class="col-12 col-md-6 col-lg-4">
  <div class="card">
    {{> reviewCard }}
    <p id="categoryTitle">{{category.title}}</p>
  </div>
</div>
{{/each}}

</div>